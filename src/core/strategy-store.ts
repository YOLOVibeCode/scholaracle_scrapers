/**
 * Re-export strategy types and orchestrator from @scholaracle/contracts (single source of truth).
 */
export {
  useStrategy,
  computeFingerprint,
  type ISelectorStep,
  type IExtractionStrategy,
  type IStrategyStore,
  type IStrategyAttempt,
} from '@scholaracle/contracts';
