# Dependencies (includes symlinks into scholarmancy — already indexed there)
node_modules/

# Build / test
dist/
coverage/
*.tsbuildinfo
.jest-cache/

# Scraper run dumps (screenshots, HTML, envelopes, logs)
output/*
!output/.gitkeep

# Local credentials
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
.vscode/
.idea/

# Lockfile
package-lock.json
